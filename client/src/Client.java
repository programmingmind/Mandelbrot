public class Client {
	public static void main(String[] args) {
		// look for settings file
		// if exists, load file
		// else, make file and setup new account

		// make connection

		// if already have work, check with server if work is still valid
		// if no work or if work is invalid, get work

		// do work (in background)
		// Thread worker = new Thread(new Worker());
		// worker.start();

		// send work

		// check for new work (loop)
	}
}